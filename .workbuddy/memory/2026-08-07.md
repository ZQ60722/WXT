# 2026-08-07 项目日志

## 项目概况
- 项目：增城荔枝文化遗产数字化平台（纯静态前端 + FastAPI 后端）
- 前端：index.html / CSS / JS / pages / img（纯静态，多页站点）
- 后端：backend/ FastAPI + uvicorn，端口 8000；路由 terminology、ai_evaluation（调硅基流动 DeepSeek API）
- 数据库：SQLAlchemy + SQLite（backend/db/lychee_culture.db）
- 关键文件：backend/main.py、database.py、routers/ai_evaluation.py、JS/tech2.js

## 上线方案决策（用户已选 A）
- **A 路线（免卡，已选）**：Koyeb 免费 Web 服务（跑后端+托管前端） + Supabase 免费 Postgres（存数据，真持久化）
- 放弃的：Koyeb 自带 Postgres（免费仅 5h/月≈不可用）、纯 Koyeb 全家桶（临时盘丢 SQLite 数据）、Hugging Face Spaces Docker（已变付费 PRO）
- 备选 B（可接受开卡）：Oracle Always Free 永久 VM，SQLite 零改动，离增城近

## 安全性（已完成，已验证）
- 3 处硬编码硅基流动 key 改为 `os.environ.get("SILICONFLOW_API_KEY")`（ai_evaluation.py×2、db/2.py×1）
- 全仓库已无明文 `sk-` 密钥（grep 验证为 0）
- 新增 .gitignore（忽略 .env、*.db、.venv 等）、.env.example
- main.py 加 load_dotenv()
- 用户已在硅基流动后台删除/轮换旧 key

## 本次（A 路线）已落盘的部署文件
- requirements.txt（fastapi/uvicorn/sqlalchemy/httpx/pydantic/python-dotenv/psycopg2-binary）
- backend/database.py：create_engine 改读 DATABASE_URL 环境变量，默认回退 SQLite
- backend/main.py：catch-all 路由托管前端（index.html/CSS/JS/pages/img，FRONTEND_DIR=../frontend）；CORS 改读 ALLOWED_ORIGINS 环境变量
- JS/tech2.js：API_BASE_URL 改为同源（生产 ''，本地自动回退 localhost:8000）
- Dockerfile：python:3.11-slim，装依赖，复制 backend+前端到 /app，从 /app/backend 启动 uvicorn
- koyeb.yaml：Koyeb 应用定义（密钥仍须在控制台填，不入库）
- .github/workflows/keepalive.yml：每 10 分钟 ping Supabase 防免费库暂停
- backend/migrate_sqlite_to_postgres.py：一次性把本地 SQLite 迁到 Postgres（DATABASE_URL）
- 所有 Python 改动已通过 py_compile 语法校验

## 待用户执行的后续步骤（手册在对话里）
1. 注册 Koyeb（免卡）+ Supabase（免卡），建空 Postgres 库
2. 本地跑迁移脚本把数据搬到 Supabase（DATABASE_URL=连接串 python backend/migrate_sqlite_to_postgres.py）
3. 把代码推 GitHub，Koyeb 连仓库用 Dockerfile 部署，端口 8000
4. Koyeb 控制台填环境变量：SILICONFLOW_API_KEY、DATABASE_URL、ALLOWED_ORIGINS
5. 开启 keepalive 的 GitHub Actions（填项目 ref 与 anon key）
6. 拿到 *.koyeb.app 网址，私发给小团体

## 备注
- 前端是相对路径引用（CSS/、JS/、pages/、img/），FastAPI 按 URL 路径映射到 frontend 目录，行为与本地静态服务器一致
- 本地开发：backend 直接从 backend/ 目录跑，FRONTEND_DIR 不存在时根路径返回 404，不影响 API
